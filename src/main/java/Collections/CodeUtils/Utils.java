package Collections.CodeUtils;

public class Utils {
    public static void main(String[] args) {
        // Printing array in 2 ways
        // Arrays.toString() & Arrays.deepToString()
        // basically collections can e printed because we use wrapper objects
        // And wrapper objects by default overrides & implements "toString()" method
        // whereas primitive arrays are not object types and cannot be printed or passed to "sout"
    }
}
